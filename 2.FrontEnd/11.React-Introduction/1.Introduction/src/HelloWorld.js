import React from 'react';

class HelloWorld extends React.Component {
  render() {
    return <span>Hello, world!</span>
  }
}

export default HelloWorld;

// escrevendo o código via classe;
// usar o componente e o render(), pra exibir na tela tem que renderizar
// ou seja, isso se torna uma classe HTML com código React embutido;
